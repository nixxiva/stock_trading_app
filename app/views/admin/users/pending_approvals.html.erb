<h1>Pending User Approvals</h1>
<ul>
  <% @pending_users.each do |user| %>
    <li>
      <%= user.email %>
      <%= link_to "Approve", approve_admin_user_path(user), 
                data: { turbo_method: :patch, turbo_confirm: "Approve this user?" } %>
    </li>
  <% end %>
</ul>
